pipeline {
    agent {
      node{
        label 'zip-job-docker' 
	privileged true 
      }
    }
    stages{
        stage('Build') {
            steps {
                sh '/tmp/is_file_exist'
            }
        }
    }    
}
